# Claude Code Entry Point

Follow `AGENTS.md`. Use `INSTALL.md` for first-run commands and load `.agents/skills/repomesh/SKILL.md` for repository mapping, session ingestion, cross-repository context, freshness checks, and Mesh synchronization.
